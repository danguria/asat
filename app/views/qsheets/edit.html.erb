<h1>Editing Qsheet for <%= @division.contest + " " + @division.division + " " + @division.round.to_s %></h1>

<%= render 'form' %>

<%= link_to 'Show', @qsheet %> |
<%= link_to 'Back', qsheets_path %>
